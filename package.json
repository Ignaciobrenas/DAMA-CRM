{
  "name": "dama-crm",
  "version": "1.0.0",
  "private": true,
  "description": "CRM Modular Open-Source y Self-Hosted para PYMES",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:core": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently -n \"API,CLIENT\" -c \"blue,cyan\" \"npm run dev:server\" \"npm run dev:client\"",
    "test": "npm --prefix server test",
    "build:server": "npm --prefix server run build",
    "build:core": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:server && npm run build:client",
    "db:setup": "npm --prefix server run db:setup",
    "db:migrate": "npm --prefix server run db:migrate",
    "db:migrate:status": "npm --prefix server run db:migrate:status",
    "db:migrate:dev": "npm --prefix server run db:migrate:dev",
    "db:seed": "npm --prefix server run db:seed",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "keywords": [
    "crm",
    "open-source",
    "self-hosted",
    "typescript",
    "react",
    "express",
    "prisma",
    "postgresql",
    "kanban",
    "unopim"
  ],
  "author": "Ignacio",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
